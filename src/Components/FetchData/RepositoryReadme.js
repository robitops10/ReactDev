import React from 'react';
import LoadReadme from './LoadReadme';


const RepositoryReadme = ( {login, repo} ) => {

	return(
		<h2>Read Readme file from Git Repository</h2>
	);
};

export default RepositoryReadme;
